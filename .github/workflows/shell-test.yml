name: Shell testing

on:
  push:
    branches: [ smolsky/testing ]

jobs:
  build:
    runs-on: macos-11

    steps:
    - shell: bash
      env:
        username: ${{ secrets.test1 }}
        password: ${{ secrets.testA }}
      run: |
        echo "%username"
        echo "%password"